[pytest]
minversion = 6.0
addopts = 
    --maxfail=5
    --disable-warnings
    --strict-markers
    --tb=short
testpaths = 
    tests
python_files = 
    test_*.py
python_functions = 
    test_*
markers =
    integration: mark integration tests (deselected by default)
    asyncio: mark a test as asyncio to be run with pytest-asyncio
