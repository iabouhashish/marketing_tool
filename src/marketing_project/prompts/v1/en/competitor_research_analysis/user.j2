Analyse the following competitor content and explain why it performs well or poorly.

───────────────────────────────────────
CONTENT DETAILS
───────────────────────────────────────

- URL: {{ url | default('N/A') }}
- Title: {{ title | default('Untitled') }}
- Content Type: {{ content_type | default('blog') }}
{% if platform %}
- Platform: {{ platform }}
{% endif %}
{% if meta_description %}
- Meta Description: {{ meta_description }}
{% endif %}

───────────────────────────────────────
CONTENT (first 3000 characters)
───────────────────────────────────────

{{ content_snippet | default('(no content available)') }}

───────────────────────────────────────
YOUR CONTEXT
───────────────────────────────────────

- Niche / Industry: {{ niche | default('general') }}
- Content Goals: {{ goals | default('increase engagement and organic traffic') }}

───────────────────────────────────────
ANALYSIS INSTRUCTIONS
───────────────────────────────────────

Analyse this content across all relevant dimensions:

1. CONTENT STRUCTURE — format, length, headings, media usage, readability
2. SEO SIGNALS — keyword targeting, meta quality, link strategy, URL structure
3. ENGAGEMENT SIGNALS — CTAs, emotional hooks, social proof, personalisation
{% if content_type == 'social_media' %}
4. SOCIAL MEDIA SIGNALS — hook quality and opening line, media type, post format, hashtag strategy
{% endif %}
5. WHAT MAKES THIS CONTENT SUCCEED — identify 2–5 specific strength factors with impact ratings
6. WHERE IT FALLS SHORT — identify 2–5 weakness factors, each with a concrete opportunity for improvement
7. ACTIONABLE INSIGHTS — 3–7 specific actions to outperform this content, tailored to the niche "{{ niche | default('general') }}" and goal "{{ goals | default('increase engagement and organic traffic') }}"

Respond with a valid JSON object matching the required schema. Do not include markdown formatting or code fences.
